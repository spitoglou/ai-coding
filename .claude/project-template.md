# Project Profile

> The single source of project-specific facts that the toolkit's **generic
> core** reads. Copied to `.claude/project.md` on first bootstrap and preserved
> across core updates.
>
> - The block between the `CORE:AUTODETECT` markers is **auto-generated** by
>   `adapt.py` / `/adapt`. Do not edit it by hand — it is regenerated on every
>   run.
> - Everything under **Project-specific notes** is yours. It is never
>   regenerated and never overwritten by `install.py`.

<!-- CORE:AUTODETECT:START -->
<!-- Populated by adapt.py — run: python3 .claude/skills/agent-coordination/scripts/adapt.py -->
<!-- CORE:AUTODETECT:END -->

## Project-specific notes

_Add the project's architecture, domain, conventions, and any deviations here.
Preserved across `adapt` reruns and core updates. Agents read this file first
for project specifics._

### Architecture

_High-level structure, key modules, entry points._

### Conventions

_Naming, branching, commit style, review expectations, definition of done._

### Domain / glossary

_Terms an agent needs to understand this codebase._

### Toolchain overrides

_If any auto-detected command in the block above is wrong or incomplete, put the
correct one here. Values here take precedence over auto-detection._

<!-- e.g. Test: make test-fast -->
